import{NextResponse}from"next/server";import{currentUser}from"@/lib/auth";import{priceForMargin}from"@/lib/margin";export async function GET(){const u=await currentUser();if(!u||u.email!==process.env.ADMIN_EMAIL)return NextResponse.json({error:"forbidden"},{status:403});const c=Number(process.env.ELEVENLABS_COST_PER_MINUTE||.15);return NextResponse.json({providerCostPerMinute:c,prices:{margin50:priceForMargin(c,.5),margin60:priceForMargin(c,.6),margin70:priceForMargin(c,.7)}})}