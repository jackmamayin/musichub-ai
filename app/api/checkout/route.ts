import{NextResponse}from"next/server";import Stripe from"stripe";import{currentUser}from"@/lib/auth";const pk:any={starter:process.env.STRIPE_PRICE_STARTER,growth:process.env.STRIPE_PRICE_GROWTH,scale:process.env.STRIPE_PRICE_SCALE};export async function POST(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:"请登录"},{status:401});const{name}=await req.json();const price=pk[name];if(!price)return NextResponse.json({error:"Stripe Price 未配置"},{status:503});const s=new Stripe(process.env.STRIPE_SECRET_KEY!);const x=await s.checkout.sessions.create({mode:"payment",line_items:[{price,quantity:1}],success_url:`${process.env.NEXT_PUBLIC_APP_URL}/?paid=1`,cancel_url:`${process.env.NEXT_PUBLIC_APP_URL}/?cancelled=1`,metadata:{userId:u.id,packageName:name}});return NextResponse.json({url:x.url})}