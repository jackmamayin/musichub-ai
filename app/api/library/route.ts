import{NextResponse}from"next/server";import{currentUser}from"@/lib/auth";import{db}from"@/lib/db";export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:"未登录"},{status:401});const a=await db.generation.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:50});return NextResponse.json({items:await Promise.all(a.map((x:any)=>({...x,audioUrl:x.storageKey?`/api/download/${x.id}`:null})))})}