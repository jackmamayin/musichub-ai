# v0.9 Release Notes

- Hyperdrive binding added.
- Prisma switched to `@prisma/adapter-pg` + `pg` driver adapter.
- Runtime database access automatically prefers `HYPERDRIVE.connectionString` on Cloudflare and falls back to `DATABASE_URL` locally.
- Added Hyperdrive create/validate scripts.
- Deployment docs updated for origin migrations vs Worker runtime.
- GitHub/Workers Builds deployment documented.
- v0.8 Queue/R2/KV architecture retained.
- No fake second AI provider added; provider adapters remain based on verified provider APIs.
